//! Topic 29 — Async / await (add a runtime crate when you start, e.g. `tokio` with `rt-multi-thread`).

fn main() {
    println!("TODO: add Tokio (or smol) and write a tiny async TCP client/server.");
    println!("Concepts: Future, Waker, Pin, Stream — map each to a small example.");
}
